%--------------------------------------
%参数解释：
%--------------------------------------
%f1：磁盘数据流读取文件1（如果输出为否，输出为真时此项无用,f1任意）
%f2：磁盘数据流读取文件2（如果输出为否，输出为真时此项无用,f2任意）
%ofn：数据流输出文件名（如果输出为真，输出为否时此项无用，ofn任意）
%fenshu：混音分数（FFV）（任何时候）
%notoutput：是否输出（如果为真(not...>1)输出至文件，如果为否输出至内存变量）
%input：音频数据流读取地址（如果为真（>1）读取内存，如果为假读取f1,f2）
%--------------------------------------
%此处读数据
%--------------------------------------
%%%%%%%%%%%%%判断读取位置
%if input>1;%读内存

%[A,fs1,bit1] = wavread(f1);
%[F,fs2,bit2] = wavread(f2);
%--------------------------------------
%此处分割出合并的地方
%--------------------------------------

%--------------------------------------
%*BUG已经修复，此段暂时无效
----------------------------------------
%if fon>1;%判断按分数相交混音是否是打开状态，大于1就是打开分数，把相交混音设为1/fenshu，如果没有打开的话那么就是按照fenshu的值直接给相交混音赋值
%另外注意一下，如果是按照分数来混音的话有可能出现越来越快直到鬼畜的情况，如果按照准确的值来混音的话这个数要尽量小，过大可能会发生分割混音的地方比整个文件还大的尴尬情况
%c=round((fenshu-1)*length(A)/fenshu);
%B=A(1:c);
%C=A(c+1:end);
%end;
%--------------------------------------
%*BUG已经修复，此段暂时无效
%--------------------------------------
%此段进行混音
%--------------------------------------

%--------------------------------------
%此段写临时文件
%--------------------------------------
%%%%%%%%%判断是否输出参数

%--------------------------------------
%此段赋值
%--------------------------------------